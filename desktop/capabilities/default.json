{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Weaver desktop shell windows. The cockpit UI runs in an external-URL WebView and does not use the Tauri IPC bridge, so only core defaults are granted.",
  "windows": ["loading", "main", "crash"],
  "permissions": ["core:default"]
}
